#ifndef JOIN_LISTS_H
#define JOIN_LISTS_H
#include <assert.h>
#include <cstdio>
#include <cstring>
#include <iostream>
#include <vector>
using namespace std;

void JOIN_TUPLES(uint64_t *, size_t, uint64_t *, size_t, uint16_t **);
void JOIN_TUPLES2(uint64_t *, size_t, uint16_t **);

#endif
